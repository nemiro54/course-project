@{
    ViewData["Title"] = "Collection Page";
}

<div class="text-center">
    <h1 class="display-4">@ViewBag.MyCollection.Name</h1>
</div>

<form id='items' method="post">
    <a type="submit" class="btn btn-outline-primary m-1" asp-controller="Items" asp-action="Index" asp-route-collectionId="@ViewBag.MyCollection.Id">Create Item</a>
    <a type="submit" class="btn btn-outline-primary m-1" asp-controller="Items" asp-action="Index" asp-route-collectionId="@ViewBag.MyCollection.Id">Delete Item</a>
    <table class="table table-striped table-hover">
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Tags</th>
            <th>
                <input type="checkbox" name="cb_all">
            </th>
        </tr>
        @if (Model != null)
        {
            @foreach (var items in Model)
            {
                <tr>
                    <td>@items.id</td>
                    <td>@items.Name</td>
                    <td>Tags</td>
                    <td>
                        <input type="checkbox" class="qwe" name="item" value=""/>
                    </td>
                </tr>
            }
        }
    </table>
</form>
<script>
    var f = document.getElementById('items');
    f.cb_all.onchange = function (e) {
        var el = e.target || e.srcElement;
        var qwe = el.form.getElementsByClassName('item');
        for (var i = 0; i < qwe.length; i++) {
            qwe[i].checked=!!el.checked;
        }
    }
</script>